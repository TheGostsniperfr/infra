routes:
  coder:
    hostnames:
      - "plex.arffornia.com"
    rules:
      - backendRefs:
          - name: plex-plex-media-server
            port: 32400
    auth:
      enabled: false
    ipFilter:
      enabled: false
