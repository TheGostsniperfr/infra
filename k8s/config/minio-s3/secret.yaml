apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: minio-credentials
  namespace: minio-s3
spec:
  path: "secret/minio-s3/credentials"
  
  keys:
    - rootUser
    - rootPassword
    
  type: Opaque