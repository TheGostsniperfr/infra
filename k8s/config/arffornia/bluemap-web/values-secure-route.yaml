routes:
  bluemap:
    hostnames:
      - "map.arffornia.com"
    rules:
      - backendRefs:
          - name: bluemap-web-service
            port: 80
    auth:
      enabled: false
    ipFilter:
      enabled: false
