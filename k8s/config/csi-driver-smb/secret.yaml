apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: vault-smb-nas-credentials-nextcloud
  namespace: default
spec:
  path: secret/smb/nas-credentials
  keys:
    - username
    - password
  type: Opaque

