apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: smb-nas-credentials
  namespace: default
spec:
  kubernetes:
    role: csi-smb-driver
    authPath: kubernetes

  path: secret/data/shared/smb/smb-credentials
  keys:
    - username
    - password
  type: Opaque

