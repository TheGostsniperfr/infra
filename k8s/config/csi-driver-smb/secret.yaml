apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: smb-nas-credentials
  namespace: default
spec:
  path: secret/smb/nas-credentials
  keys:
    - username
    - password
  type: Opaque

