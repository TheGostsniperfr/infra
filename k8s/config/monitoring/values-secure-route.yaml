routes:
  coder:
    hostnames:
      - "grafana.arffornia.com"
    rules:
      - backendRefs:
          - name: kube-prometheus-stack-grafana
            port: 80
    auth:
      enabled: false
    ipFilter:
      enabled: false
