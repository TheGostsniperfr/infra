apiVersion: v1
kind: Service
metadata:
  name: postgres-operator-ui
  namespace: postgres-operator
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: postgres-operator-ui
  type: ClusterIP
